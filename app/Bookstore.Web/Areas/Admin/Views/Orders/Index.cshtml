@using Bookstore.Domain.Orders;
@using Bookstore.Web.Helpers;
@using Microsoft.AspNetCore.Mvc.Rendering;

@model Bookstore.Web.Areas.Admin.Models.Orders.OrderIndexViewModel

@{ ViewData["Title"] = "Orders"; }

<div class="d-flex m-3">
    <h5 class="me-auto">Orders</h5>
</div>

<div class="card mx-3">
    <div class="card-body">
        <form class="row row-cols-lg-auto g-3 align-items-center" method="get">
            <div class="col-12">
                <label class="visually-hidden" for="OrderStatusSelect">Reference data type</label>
                <select asp-for="Filters.OrderStatusFilter" asp-items="Html.GetEnumSelectList<OrderStatus>()" class="form-select">
                    <option value="">All Order Statuses</option>
                </select>
            </div>
            <div class="col-12">
                <label class="visually-hidden" for="OrderStatusSelect">Order date from</label>
                <input asp-for="Filters.OrderDateFromFilter" class="form-control" type="date" />
            </div>

            <div class="col-12">
                <label class="visually-hidden" for="OrderStatusSelect">Order date to</label>
                <input asp-for="Filters.OrderDateToFilter" class="form-control" type="date" />
            </div>

            <div class="col-12">
                <button type="submit" class="btn btn-primary">Filter</button>
            </div>

            <div class="col-12">
                <a asp-controller="Orders" asp-action="Index" class="btn btn-secondary">Clear</a>
            </div>
        </form>
    </div>
</div>

<div class="card m-3">
    <div class="card-header">
        <div class="d-flex justify-content-end">
            @await Html.PartialAsync("_Paginator")
        </div>
    </div>

    <div class="card-body">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">Customer Name</th>
                    <th scope="col">Status</th>
                    <th scope="col">Order Date</th>
                    <th scope="col">Delivery Date</th>
                    <th scope="col">Order Total</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var order in Model.Items)
                {
                    <tr>
                        <td>@order.CustomerName</td>
                        <td>@order.OrderStatus</td>
                        <td>@order.OrderDate</td>
                        <td>@order.DeliveryDate.ToShortDateString()</td>
                        <td>@order.Total.ToString("C")</td>
                        <td>
                            <a asp-controller="Orders" asp-action="Details" asp-route-id="@order.Id" class="card-link">View</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="card-footer">
        <div class="d-flex justify-content-end">
            @await Html.PartialAsync("_Paginator")
        </div>
    </div>
</div>
